export type CourseBodyType = {
  name: string;
  description: string;
  category: string;
  price: number;
};

export type CourseFilesType = {
  intro_video: string;
  thumbnail: string;
  video: string;
  ppt: string;
  pdf: string;
};
